# Pinned against Python 3.14 (see Dockerfile).
# The previous mid-2024 pins could not install on 3.14: langchain 0.2.x caps
# numpy<2 and chromadb 0.5.x needs chroma-hnswlib, and neither has a 3.14 wheel.

# --- API ---------------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.4

# --- RAG orchestration -------------------------------------------------
# langchain 1.x: import paths differ from the 0.2 line the README was written
# against (e.g. chains/agents moved to langchain-classic).
langchain==1.3.18
langchain-google-genai==4.4.0
langchain-community==0.4.2

# --- Vector store & embeddings -----------------------------------------
chromadb==1.5.9
sentence-transformers==6.0.1

# --- Documents ---------------------------------------------------------
pypdf==6.16.2

# --- Runtime -----------------------------------------------------------
python-dotenv==1.2.3
loguru==0.7.3

# --- Dev ---------------------------------------------------------------
pytest==9.1.1
